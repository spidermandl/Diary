package com.diary.goal.toolkit.wheel;

import android.content.Context;
import android.graphics.Canvas;
import android.util.AttributeSet;

public class CalendarWheelView extends WheelView {

	public CalendarWheelView(Context context) {
		super(context);
	}
	public CalendarWheelView(Context context, AttributeSet attrs) {
		super(context, attrs);
	}
	public CalendarWheelView(Context context, AttributeSet attrs, int defStyle) {
		super(context, attrs, defStyle);
	}

	@Override
	protected void drawShadows(Canvas canvas) {
		
	}
}
